[phases.setup]
nixPkgs = ["graphviz", "cairo", "pango", "gdk-pixbuf", "libffi"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn main:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --log-level debug"
